#include <iostream>
#include "../include/bitboard.h"
int main()
{
    U64 bitboard = 1ULL;
    map_leap_attacks();
    // for (int i = 0; i < 8; i++)
    // {
    //     print_bitboard(mask_rook_attacks(i));
    // }
    // // print_bitboard(mask_rook_attacks(d4));
    return 0;
}